<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Donate to DiscoTECA" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="DiscoTECA Donations">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="font-['Inter',system-ui,sans-serif] antialiased text-gray-900 bg-gray-50">
    <header class="border-b border-gray-200 bg-white">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-center py-4">
          <span class="text-lg font-semibold text-gray-900">
            DiscoTECA Donation Offer
          </span>
        </div>
      </div>
    </header>

    <% if notice %>
      <div class="bg-green-50 border-b border-green-200">
        <div class="mx-auto max-w-7xl px-4 py-3 sm:px-6 lg:px-8">
          <p class="text-sm font-medium text-green-800"><%= notice %></p>
        </div>
      </div>
    <% end %>

    <% if alert %>
      <div class="bg-red-50 border-b border-red-200">
        <div class="mx-auto max-w-7xl px-4 py-3 sm:px-6 lg:px-8">
          <p class="text-sm font-medium text-red-800"><%= alert %></p>
        </div>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>